Assumptions:
1. The reporter-tests workflow passed because it only runs tests.
2. It produced no reports because that workflow intentionally had no report-rendering step and no upload-artifact step.
3. The correct fix is a separate artifact-producing workflow, not changing the test workflow.
4. This bundle adds only one workflow file.
5. The leading dot has been removed inside the bundle path for iPhone visibility.

Done definition:
1. github/workflows/report-artifact.yml exists.
2. The workflow runs classifier and reporter tests.
3. The workflow creates a deterministic demo findings input.
4. The workflow runs the classifier and writes reports/classifier_summary.json.
5. The workflow runs the reporter and writes reports/footprint_audit_report.md, reports/footprint_audit_report.json, and receipts/footprint_audit_receipts.jsonl.
6. The workflow uploads all generated reports/receipts as downloadable artifacts.

Expected artifact:
footprint-auditor-report-artifacts
